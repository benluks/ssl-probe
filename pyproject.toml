[project]
name = "ssl-probe"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "lightning>=2.6.5",
    "opensmile>=2.6.0",
    "quick-convert",
    "torchmetrics>=1.9.0",
    "transformers>=5.14.1",
]

[tool.uv.sources]
quick-convert = { path = "../psst-main", editable = true }
