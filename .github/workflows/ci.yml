name: CI

on:
  pull_request:
  push:
    branches:
      - main
      - "llm/**"

permissions:
  contents: read

jobs:
  quality:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: astral-sh/setup-uv@v6
        with:
          python-version: "3.11"
          enable-cache: true
      - name: Install development environment
        run: uv sync --group dev
      - name: Check formatting
        run: uv run --no-sync ruff format --check .
      - name: Lint
        run: uv run --no-sync ruff check --select B,E4,E7,E9,F,I,UP .
      - name: Run fast tests
        run: uv run --no-sync pytest
      - name: Verify installed wheel
        run: |
          uv build --wheel
          python -m venv /tmp/ssl-probe-wheel-smoke
          /tmp/ssl-probe-wheel-smoke/bin/pip install --no-deps dist/*.whl
          cd /tmp
          /tmp/ssl-probe-wheel-smoke/bin/ssl-probe --help | grep precompute
